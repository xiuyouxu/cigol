define(['app'], function(app) {
    app.register.controller('appIndex.example', ['$scope', '$state', '$global_var', function($scope, $state, $global_var) {
    	// go to the default page
    	// $state.go('.test')
    }]);
});